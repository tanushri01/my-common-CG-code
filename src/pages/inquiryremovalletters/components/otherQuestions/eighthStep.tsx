import React from 'react';

import { Typography } from '@/components';
import { StepBox } from '@/styles';

/**
 * @page {EighthStep} - EighthStep for the form
 * @return {JSX.Element}
 */
const EighthStep = (): JSX.Element => (
  <StepBox>
    <Typography variant="h3" color="primary">
      If you can identify the errors, you can purchase our credit repair letter
      templates.
    </Typography>
  </StepBox>
);

export default EighthStep;
