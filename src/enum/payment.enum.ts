export enum PaymentEnum {
  PROCESSING = 'processing',
  REQUIRED_PAYMENT_METHOD = 'requires_payment_method',
  REQUIRED_CONFIRMATION = 'requires_confirmation',
  REQUIRED_ACTION = 'requires_action',
  SUCCEEDED = 'succeeded',
  ERROR = 'error',
  INITIAL = 'initial',
  ONE_TIME = 'oneTime',
  USD = 'usd',
  ACTIVE = 'active',
  PAID = 'paid',
  MONTH = 'month',
}
